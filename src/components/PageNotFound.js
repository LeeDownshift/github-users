import React from "react";
import { Alert } from 'react-bootstrap';

const PageNotFound = () => {
  return(
    <Alert variant="danger">Oops! Page not found.</Alert>
  );
};

export default PageNotFound;
